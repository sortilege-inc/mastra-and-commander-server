/// <reference types="vite/client" />

/** Package version, injected by vite.config.ts `define`. Used to stamp recorded
 *  game transcripts with the app / AI version (training-data provenance). */
declare const __APP_VERSION__: string
